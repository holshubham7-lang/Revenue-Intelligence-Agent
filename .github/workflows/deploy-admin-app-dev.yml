name: Deploy Admin App (dev)

on:
  push:
    branches: [development]
    paths: ['admin/app/**']
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_ADMIN_API_URL: https://revops-admin-api-dev.azurewebsites.net
    defaults:
      run:
        working-directory: admin/app
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: admin/app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Login to Azure
        uses: Azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to App Service
        uses: Azure/webapps-deploy@v3
        with:
          app-name: revops-admin-app-dev
          resource-group-name: stratvedaos_group
          package: admin/app